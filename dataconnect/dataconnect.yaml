specVersion: "v1"
serviceId: "gestor-licencias-firebase"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "gestor-licencias-firebase-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
